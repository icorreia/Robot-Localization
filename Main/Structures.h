/* The structures that are used by the localizer, namely information about
 * the vertices, the edges and so on.
 */

typedef struct STRUCT_VERTIX{
	double x, y;
}Vertix; 
 
typedef struct STRUCT_EDGE{
	double width, length;
}Edge;
